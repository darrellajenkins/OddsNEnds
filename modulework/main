# This file calls from two different modules.  Both are required because data in mod2 relies on data from mod1.

import mod1
import mod2

myu = mod2.Admin(3000 'Rowan', 'Gradidwell', 'rowan@themail.com', 'annual', 'Miami, FL', 'Venmo')

myu.display_privileges()
